# Build context is this directory, so everything here is backend-only.

# python
.venv
.python-version
**/__pycache__
**/*.pyc
**/*.pyo

# the trained model lives in Postgres now, so the committed joblib is dead
# weight in the image
models/

# not needed to run the API
.env
.pytest_cache
.ruff_cache
*.md
